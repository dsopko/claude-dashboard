namespace ClaudeDashboard.App;

/// <summary>
/// Entry point placeholder for the Windows host.
/// </summary>
/// <remarks>
/// T1.0 is scaffolding only: this exists because <c>WinExe</c> requires an entry
/// point, and nothing more. The Generic Host, Serilog, configuration, and the
/// WPF application lifecycle (Impl 3.1, 5.1, 10.1) arrive in T1.7.
/// </remarks>
internal static class Program
{
    [STAThread]
    internal static void Main()
    {
        // Intentionally empty — see T1.7 (Generic Host bootstrap).
    }
}
